import 'package:flutter/material.dart';

class CheckboxSampleScreen extends StatelessWidget {
  const CheckboxSampleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Checkbox')),
      body: const Center(child: Text('Exemplos de Checkbox')),
    );
  }
}