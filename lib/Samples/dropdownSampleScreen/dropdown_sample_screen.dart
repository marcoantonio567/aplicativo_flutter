import 'package:flutter/material.dart';

class DropdownSampleScreen extends StatelessWidget {
  const DropdownSampleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dropdown')),
      body: const Center(child: Text('Exemplos de Dropdown')),
    );
  }
}