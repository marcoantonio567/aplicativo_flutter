import 'package:flutter/material.dart';

class NotificationSampleScreen extends StatelessWidget {
  const NotificationSampleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Notification Input')),
      body: const Center(child: Text('Exemplos de Notification Input')),
    );
  }
}